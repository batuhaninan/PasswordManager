    bool signup(std::vector<std::string> data);
    bool login( std::vector<std::string> data);


    int addNewPassword(std::vector<std::string> data);
    bool deletePassword(std::string app_name);
    bool updatePassword(std::vector<std::string> data);
    std::vector<std::string> getPassword(std::string app_name);

    std::vector<std::vector<std::string>> userData();



    signup
        it takes data vector that include username, email, password
        if email or username does it exist return false
        else insert users table and return true

    login
        it takes data that include username and password
        it checks the database username exists 
        if exist compare password if is right return true else return false

    addNewPassword 
        it takes data vector that include username, password, appname
        if appname does it exist return false
        else insert password table and return true

    deletePassword
        it take appname and delete row from password table

    updatePassword
        it take data vector that include new username, new password, appname
        find the row with appname belong the logged user, update username and password 

    getPassword
        it take appname return username and password belong the appname and logged user

    userData
        it takes nothing
        return all password data belong the logged user
 